// Route table placeholder — Phase 6 wires up react-router here and each
// subsequent UI phase (7 auth, 8 goals, 9 exchange) appends its own routes,
// so parallel phases only add lines rather than restructure this file.
//
// import { createBrowserRouter } from 'react-router';
// export const router = createBrowserRouter([
//   // auth routes (Phase 7)
//   // goals routes (Phase 8)
//   // exchange routes (Phase 9)
// ]);

export {};
